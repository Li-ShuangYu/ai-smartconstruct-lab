services:
  # 纯粹的向量检索引擎
  qdrant:
    image: qdrant/qdrant:latest
    container_name: ai-lab-qdrant
    restart: always
    ports:
      - "6333:6333"   # REST API 端口
      - "6334:6334"   # gRPC 端口 (极速通信)
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data: